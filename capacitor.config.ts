import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pl.ewronopluted.app',
  appName: 'www',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
